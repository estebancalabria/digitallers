import { createContext } from "react";

//Un contexto es una forma de pasar datos como parametros a un componente
//sin necesidad que este lo reciba por props
//"Un contexto global"
export default createContext([]);